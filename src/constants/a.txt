Math.pow(10, token.decimals)

balance = 1000000000;
const a = new Bignumber(balance)


displayvlaue = a.div(Math.pow(10, token.decimals))


amout = docuemt.get()

const b = new Bignumber(amount)


b.mul(Math.pow(10, token.decimals))